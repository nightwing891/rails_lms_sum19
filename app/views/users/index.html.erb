<h1>Users</h1>

<ul>
  <% @users.each do |user| %>
    <li>  
                                  # user_path(user)
                                  # user_path(user.id)
      <%= link_to user.full_name, user %>
    </li>
  <% end %>
</ul>
